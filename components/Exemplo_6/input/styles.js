import {} from 'react-native';

const styles = StyleSheet.create ({
    input:{
     heigh:80,
     textalign: 'center',
     width: '50%',
     color: 'lightgray',
     fontSize:50,
    },
});

export default styles;