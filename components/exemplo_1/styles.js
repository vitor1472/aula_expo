import { StyleSheet } from 'react-native';  

const styles = StyleSheet.create({
    container: {
        borderWidth: 2, 
        padding: 18, 
        backgroundColor: '#fff', 
        flex: 1, 
        alignItems: 'center'
    }, 
    texto: {
        fontSize: 30, 
    }
});

export default styles;